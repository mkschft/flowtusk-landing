{
  "name": "flowtusk-website",
  "version": "1.0.0",
  "description": "Flowtusk marketing website",
  "scripts": {
    "dev": "node server.js & npx browser-sync start --proxy 'localhost:3000' --files '*.html, css/*.css, js/*.js' --port 3001 --no-open"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}
